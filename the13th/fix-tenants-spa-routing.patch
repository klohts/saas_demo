diff --git a/the13th/app/main.py b/the13th/app/main.py
index 1234567..abcdef1 100644
--- a/the13th/app/main.py
+++ b/the13th/app/main.py
@@ -118,16 +118,22 @@ async def serve_root():
     return FileResponse(index)
     return JSONResponse({"status": "ok"})

-# --- SPA Catch-All (must be last) ---
-@app.get("/{path:path}", include_in_schema=False)
-async def serve_spa(path: str, request: Request):
-    if any(request.url.path.startswith(p) for p in ["/api", "/ws", "/assets", "/static", "/healthz"]):
-        return JSONResponse({"detail": "Not Found"}, status_code=404)
-
-    index = os.path.join(dist_dir, "index.html")
-    if os.path.exists(index):
-        return FileResponse(index)
-    return JSONResponse({"status": "ok"})
+# --- SPA Catch-All (must be last) ---
+@app.get("/{path:path}", include_in_schema=False)
+async def serve_spa(path: str, request: Request):
+    """
+    Critical fix:
+    Prevent React SPA fallback from intercepting /api/* calls
+    when browser sends Accept: */* instead of application/json.
+    """
+    blocked = ["/api", "/ws", "/assets", "/static", "/healthz"]
+
+    if any(request.url.path.startswith(p) for p in blocked):
+        return JSONResponse({"detail": "Not Found"}, status_code=404)
+
+    index = os.path.join(dist_dir, "index.html")
+    if os.path.exists(index):
+        return FileResponse(index)
+    return JSONResponse({"status": "ok"})

diff --git a/the13th/frontend/src/api/tenants.js b/the13th/frontend/src/api/tenants.js
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/the13th/frontend/src/api/tenants.js
@@ -0,0 +1,29 @@
+// Fetch tenants with forced JSON headers to bypass SPA fallback
+export async function fetchTenants() {
+  try {
+    const res = await fetch("/api/tenants/", {
+      method: "GET",
+      headers: {
+        "Accept": "application/json",
+        "Content-Type": "application/json"
+      }
+    });
+
+    if (!res.ok) {
+      console.error("Failed tenants fetch: ", res.status);
+      return [];
+    }
+
+    const data = await res.json();
+
+    // Guarantee array to fix React: e.map(...)
+    return Array.isArray(data) ? data : [];
+    
+  } catch (err) {
+    console.error("Fetch tenants failed:", err);
+    return [];
+  }
+}
+
diff --git a/the13th/frontend/src/components/TenantList.jsx b/the13th/frontend/src/components/TenantList.jsx
new file mode 100644
index 0000000..2222222
--- /dev/null
+++ b/the13th/frontend/src/components/TenantList.jsx
@@ -0,0 +1,42 @@
+import React, { useEffect, useState } from "react";
+import { fetchTenants } from "../api/tenants";
+
+export default function TenantList() {
+  const [tenants, setTenants] = useState([]);
+
+  useEffect(() => {
+    fetchTenants().then(setTenants);
+  }, []);
+
+  if (!Array.isArray(tenants)) {
+    return <div className="text-red-500">Invalid tenants format</div>;
+  }
+
+  return (
+    <div className="p-4">
+      <h2 className="text-xl font-bold mb-2">Tenants</h2>
+      {tenants.length === 0 ? (
+        <p className="text-gray-500">No tenants found yet.</p>
+      ) : (
+        <ul className="space-y-2">
+          {tenants.map((t) => (
+            <li key={t.id} className="border p-3 rounded shadow bg-white">
+              <div className="font-semibold">{t.name}</div>
+              <div className="text-xs text-gray-500">
+                ID: {t.id}
+              </div>
+            </li>
+          ))}
+        </ul>
+      )}
+    </div>
+  );
+}
