# Gmail AI — SaaS Demo

This demo wraps your Gmail automation app into a SaaS-ready structure.

## Quickstart
1. Copy `.env.example` to `.env` and fill values.
2. `pip install -r requirements.txt`
3. `make run` (API) and `make admin` (dashboard)
